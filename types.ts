export interface Destination {
  id: string;
  title: string;
  tag: string;
  image: string;
}

export interface JournalEntry {
  id: string;
  title: string;
  excerpt: string;
  category: string;
  image: string;
}